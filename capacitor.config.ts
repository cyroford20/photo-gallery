import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.photogallery',
  appName: 'Moments Photo Gallery',
  webDir: 'dist'
};

export default config;
